// Code generated by Wire protocol buffer compiler, do not edit.
// Source: com.venbiasa.wailo.protocol.RuleSet in wailo/capture.proto
import Wire

/**
 * --- Map Local (desktop <-> device control channel) ---
 * The active Map Local rules, as match-metadata only — no response bodies (ADR-0019). Sent as a
 * snapshot: each push replaces the device's set entirely (idempotent), so there is no add/remove
 * delta. [epoch] is a monotonic version the desktop owns; the device echoes it in a RuleAck so the
 * desktop can detect a lost push and re-push (anti-entropy). The device applies every snapshot it
 * receives regardless of epoch — epoch is only for ack-matching/retry — so a desktop restart that
 * resets the counter is harmless.
 */
public struct RuleSet {

    public var rules: [MapLocalRule] = []
    public var epoch: UInt64
    public var unknownFields: UnknownFields = .init()

    public init(epoch: UInt64, configure: (inout Self) -> Swift.Void = { _ in }) {
        self.epoch = epoch
        configure(&self)
    }

}

#if !WIRE_REMOVE_EQUATABLE
extension RuleSet : Equatable {
}
#endif

#if !WIRE_REMOVE_HASHABLE
extension RuleSet : Hashable {
}
#endif

extension RuleSet : Sendable {
}

extension RuleSet : ProtoMessage {

    public static func protoMessageTypeURL() -> String {
        return "type.googleapis.com/com.venbiasa.wailo.protocol.RuleSet"
    }

}

extension RuleSet : Proto3Codable {

    public init(from protoReader: ProtoReader) throws {
        var rules: [MapLocalRule] = []
        var epoch: UInt64 = 0

        let token = try protoReader.beginMessage()
        while let tag = try protoReader.nextTag(token: token) {
            switch tag {
            case 1: try protoReader.decode(into: &rules)
            case 2: epoch = try protoReader.decode(UInt64.self, encoding: .variable)
            default: try protoReader.readUnknownField(tag: tag)
            }
        }
        self.unknownFields = try protoReader.endMessage(token: token)

        self.rules = rules
        self.epoch = epoch
    }

    public func encode(to protoWriter: ProtoWriter) throws {
        try protoWriter.encode(tag: 1, value: self.rules)
        try protoWriter.encode(tag: 2, value: self.epoch, encoding: .variable)
        try protoWriter.writeUnknownFields(unknownFields)
    }

}

#if !WIRE_REMOVE_CODABLE
extension RuleSet : Codable {

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringLiteralCodingKeys.self)
        self.rules = try container.decodeProtoArray(MapLocalRule.self, forKey: "rules")
        self.epoch = try container.decode(stringEncoded: UInt64.self, forKey: "epoch")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringLiteralCodingKeys.self)
        let includeDefaults = encoder.protoDefaultValuesEncodingStrategy == .include

        if includeDefaults || !self.rules.isEmpty {
            try container.encodeProtoArray(self.rules, forKey: "rules")
        }
        if includeDefaults || self.epoch != 0 {
            try container.encode(stringEncoded: self.epoch, forKey: "epoch")
        }
    }

}
#endif
