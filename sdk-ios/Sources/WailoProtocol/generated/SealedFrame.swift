// Code generated by Wire protocol buffer compiler, do not edit.
// Source: com.venbiasa.wailo.protocol.SealedFrame in wailo/transport.proto
import Foundation
import Wire

/**
 * Every frame after a successful WiFi handshake. seq is the AES-GCM nonce counter, unique per
 * direction and per session, and must arrive strictly increasing — a repeat is a replay and a gap
 * means something other than TCP is delivering these frames.
 */
public struct SealedFrame {

    public var seq: UInt64
    public var ciphertext: Foundation.Data
    public var unknownFields: UnknownFields = .init()

    public init(seq: UInt64, ciphertext: Foundation.Data) {
        self.seq = seq
        self.ciphertext = ciphertext
    }

}

#if !WIRE_REMOVE_EQUATABLE
extension SealedFrame : Equatable {
}
#endif

#if !WIRE_REMOVE_HASHABLE
extension SealedFrame : Hashable {
}
#endif

extension SealedFrame : Sendable {
}

extension SealedFrame : ProtoMessage {

    public static func protoMessageTypeURL() -> String {
        return "type.googleapis.com/com.venbiasa.wailo.protocol.SealedFrame"
    }

}

extension SealedFrame : Proto3Codable {

    public init(from protoReader: ProtoReader) throws {
        var seq: UInt64 = 0
        var ciphertext: Foundation.Data = .init()

        let token = try protoReader.beginMessage()
        while let tag = try protoReader.nextTag(token: token) {
            switch tag {
            case 1: seq = try protoReader.decode(UInt64.self, encoding: .variable)
            case 2: ciphertext = try protoReader.decode(Foundation.Data.self)
            default: try protoReader.readUnknownField(tag: tag)
            }
        }
        self.unknownFields = try protoReader.endMessage(token: token)

        self.seq = seq
        self.ciphertext = ciphertext
    }

    public func encode(to protoWriter: ProtoWriter) throws {
        try protoWriter.encode(tag: 1, value: self.seq, encoding: .variable)
        try protoWriter.encode(tag: 2, value: self.ciphertext)
        try protoWriter.writeUnknownFields(unknownFields)
    }

}

#if !WIRE_REMOVE_CODABLE
extension SealedFrame : Codable {

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringLiteralCodingKeys.self)
        self.seq = try container.decode(stringEncoded: UInt64.self, forKey: "seq")
        self.ciphertext = try container.decode(stringEncoded: Foundation.Data.self, forKey: "ciphertext")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringLiteralCodingKeys.self)
        let includeDefaults = encoder.protoDefaultValuesEncodingStrategy == .include

        if includeDefaults || self.seq != 0 {
            try container.encode(stringEncoded: self.seq, forKey: "seq")
        }
        if includeDefaults || !self.ciphertext.isEmpty {
            try container.encode(stringEncoded: self.ciphertext, forKey: "ciphertext")
        }
    }

}
#endif
